# spring.application.name=smartdelivery

spring:
  application:
    name: smartdelivery

  datasource:
    url: jdbc:postgresql://localhost:5432/smartdelivery
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
